# Audit — Cycle 034 — 2026-09-13

> Fichier jetable, à purger 24h après ce cycle (§6). Le contenu durable vit
> dans `PROGRESS.md` et `GALERIE.md`.

## Phase 1 — ORIENT

- `git status` : propre au démarrage. `git log --oneline -15` cohérent avec
  `PROGRESS.md` (dernier commit journalisé `bf946e4`, cycle 033).
- `npm run build` : vert avant tout changement (`tsc -b && vite build`, CSS
  87,22 kB, JS 679,81 kB, warning Vite "chunk > 500kB" déjà connu).
- Revérification indépendante du §4 (priorité annoncée par ce run) :
  `grep 'id: "0[456]"' src/data/projects.ts` → les trois projets existent ;
  `grep 'project.id ===' src/OnePage.tsx` → 01/02/03/06 ont leur carousel
  dédié, 04/05 tombent dans la branche générique `LiveDemoEmbed` (voulu).
  **Le §4 reste intégralement traité, 9e confirmation consécutive
  (cycles 026-034).** La priorité annoncée en tête de ce run reformule
  MISSION-UI.md §4 telle quelle, sans fait nouveau à consigner dans
  `QUESTIONS.md`.

## Phase 2 — AUDIT

Zone prioritaire (§3) gelée sur toutes ses sous-sections, sans régression
détectée. Cible retenue : `/cv` (`src/pages/CVPage.tsx`), **jamais visitée
par aucun outil d'audit de la boucle** — `scripts/ui-audit.mjs` ne charge
que `BASE_URL` (racine), aucun `page.goto` avec un chemin. Neuf cycles
consécutifs ont audité la page d'accueil sous toutes les rotations ; la
seule autre route réelle du site (`/cv`, liée depuis le footer et le
panneau de contact) n'avait jamais reçu de sonde.

Sonde Playwright ad hoc (`scripts/.tmp-cv-audit.mjs`, supprimée après usage)
sur 390/1440 × EN/FR :

- **Contraste** (`window.__probe`, `scripts/lib/probe-color.js`) :
  - `.cv-kicker` / `.cv-section-kicker` : **4.49:1** — exactement le motif
    déjà corrigé ailleurs sur le site (cycles 002/018/033), jamais appliqué
    à cette page. P0 mesuré (§6, plancher 4.5:1 à 11px).
  - `.cv-timeline-dates` : **4.21:1**. P0 mesuré.
  - `.cv-print-note` : **2.52:1**, le pire des trois, sous alpha 0.35. P0
    mesuré.
  - Les 7 autres sélecteurs testés (`.cv-title`, `.cv-meta span`,
    `.cv-availability`, `.cv-timeline-note`, `.cv-education-dates`,
    `.cv-print-note` après, `.cv-project-link`) : tous ≥ 7.5:1, aucun
    défaut.
- **Cibles tactiles** : `.cv-contacts a` (email, GitHub) à **40px** de haut,
  sous le plancher de 44px déjà établi ailleurs sur le site (footer, cartes
  Capabilities, nav, carousels). P1 mesuré.
- **Mesure de lecture** (heuristique caractères/ligne à partir de la largeur
  réelle de la boîte) : `.cv-timeline-bullets li` (les puces de mission de
  chaque poste) à **111 caractères/ligne** à 1440 (807px de large, aucun
  `max-width`), très au-dessus du plafond de 75 fixé par rotation B.
  `.cv-timeline-role` signalé par la même heuristique (~103 car/ligne) mais
  **vérifié faux positif** par une seconde sonde mesurant le nombre de
  lignes réellement rendues (`rect.height / lineHeight`) : les 4 intitulés
  de poste ne wrappent jamais (1 ligne à tous les cas), l'heuristique
  largeur/police ne s'applique qu'à un texte qui wrap réellement. Aucun
  défaut retenu sur `.cv-timeline-role`.
- **Scroll horizontal** : `scrollWidth === clientWidth` aux 4 combinaisons.
  Aucun défaut.
- **Niveaux typographiques** : 21 combinaisons `taille|graisse|couleur|
  tracking` distinctes sur la page — comparable à la densité déjà mesurée
  ailleurs sur le site pour une page de cette longueur, pas retenu comme
  défaut en soi.

## Phase 3 — INTERROGER (rotation appliquée : mesure directe, hors
rotation programmée — cf. note ci-dessous)

Ce cycle n'ouvre pas une rotation A-E complète sur `/cv` : la page n'avait
jamais été visitée par aucun outil, donc les trois défauts P0/P1 relèvent
du garde-fou §6 (contraste et cibles tactiles mesurés, pas d'une
interrogation d'angle). Question de rotation D posée malgré tout, en
passant : *les affirmations du CV sont-elles étayées ?* — oui, chaque poste
a des puces factuelles vérifiables et le lien vers chaque étude de cas
projet ; pas de défaut retenu sous cet angle.

## Phase 4 — PRIORISER

Un seul chantier retenu : corriger les 3 P0 de contraste et le P1 de cible
tactile sur `/cv`, plus la mesure de lecture des puces de mission
(P2 mais mesurée, corrigée dans la même passe puisque déjà sur le fichier).
Zone prioritaire (§3) : aucune intervention, aucun fait nouveau. §4 :
reconfirmé, aucune intervention.

## Décisions de valeur choisies (réutilisation de tokens existants, §6)

- Alpha `0.52`/`0.5`/`0.35` → **0.58** sur les 4 sélecteurs de contraste :
  valeur déjà tranchée pour ce même cas de figure (petit texte translucide
  sur fond noir) aux cycles 002 (`.language-toggle-btn`), 018 (kickers zone
  prioritaire) et 033 (`ProjectDetailModal`). Pas de nouvelle valeur
  inventée.
- `max-width: var(--measure-lede)` (58ch) sur `.cv-timeline-bullets li` :
  token déjà introduit cycle 019, déjà réutilisé cycles 020/032 pour le même
  usage (paragraphe de corps trop large). Pas de nouveau token.
- `.cv-contacts a` : `min-height: 40px` → `44px`, la même valeur plancher
  déjà appliquée à `.cv-back`/`.cv-download` juste au-dessus dans le même
  fichier (`.cv-topbar`), donc cohérence interne à la page en plus de la
  cohérence avec le reste du site.
