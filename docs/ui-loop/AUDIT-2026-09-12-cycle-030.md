# Audit — Cycle 030 — 2026-09-12

**Zone auditée** : hors zone prioritaire (§3, gelée) et hors §4 (reconfirmé intégralement
traité) — hero (`.city-nav`, `.city-content`, `#profile`), et un slide projet
représentatif (`#project-01 .home-project-card`, gabarit partagé par les 6 cartes).
**Rotation** : **A — Hiérarchie** (seule des cinq jamais posée par écrit hors zone
prioritaire, point de reprise explicite du cycle 029).

## Note de continuité — §4 revérifié sur le code, pas sur le seul journal

`grep 'id: "0[456]"' src/data/projects.ts` → les trois entrées existent.
`grep 'project.id ===' src/OnePage.tsx` → 01/02/03/06 ont leur carousel dédié,
04/05 tombent dans la branche générique `demoLink ? <LiveDemoEmbed>` (voulu).
`npm run build` vert avant tout changement. **Le §4 reste intégralement traité**,
reconfirmé pour la 5e fois consécutive (cycles 026-030). Conformément à l'ordre
de priorité §2 phase 4, aucun P0 réel trouvé et §3 reste P2 gelée sans fait
nouveau → cycle retombé sur « P2 — reste du site », rotation A.

## Outillage

`scripts/ui-hierarchy-probe.mjs` (jusqu'ici limité à `#about`/`#capabilities`/
`#contact`/`.site-footer`) étendu à `.city-nav`, `.city-content`, `#profile` et
`#project-01 .home-project-card`. Run complet : 4 viewports × 2 langues, rapport
dans `docs/ui-loop/screenshots/hierarchy-cycle030/report.json`.

## Constats

- **`city-nav`** : 2-3 niveaux typo selon viewport, salience du premier écran
  dominée par "Contact" (22 880) devant "AXEL"/logo (5 508) — cohérent avec un
  visiteur qui veut d'abord repérer la sortie de conversion. Rien à corriger.
- **`city-content`** (état "ville" du hero) et **`profile`** (état "falaise") :
  5 et 8 niveaux respectivement, salience dominée par le titre d'affichage dans
  les deux cas — c'est la hiérarchie attendue. Un near-duplicate relevé sur
  `profile` à 390 seulement (12px, luminance 0.3247 vs 0.3606, delta 0.036 —
  sous le seuil de 0.06) entre le sous-titre de rôle et le kicker "Personal
  layer" : deux libellés différents, deux rôles différents (identité vs
  section), une teinte de gris presque identique par coïncidence à cette seule
  largeur. Pas un défaut mesurable (aucune collision, aucun texte tronqué ou
  confondu) — noté pour mémoire, aucun chantier ouvert.
- **P2, corrigé ce cycle — redondance de contenu entre la copie statique de
  carte et le slide "cover" du carousel.** `project-card-01` remontait **18
  niveaux typographiques distincts sur une seule carte** — au-dessus du repère
  "plus de 4 = bruit" de la mission (repère pensé pour un bloc simple de la
  zone prioritaire, une carte de cas d'usage dense a légitimement plus de
  rôles ; mais le nombre a poussé à lire le détail plutôt qu'à l'écarter comme
  normal). En creusant les échantillons : deux nœuds à la même taille (17.6px,
  400, Almarai) portaient deux phrases différentes — `project.description`
  (texte statique, toujours visible) et le champ `sub` du slide "cover" du
  carousel (visible par défaut, avant toute interaction) — qui redisent
  quasiment la même chose ("end-to-end pipeline… ingestion… modèle").
  Généralisé aux 4 projets à carousel (01/02/03/06, EN+FR) :
  - **02 — JobTrackr** : `sub` = "Aggregates job offers, scores them against my
    CV, and tracks every application." contre `description` = "…automatic job
    aggregation from multiple sources, application tracking and AI-based
    CV/job compatibility scoring." — mêmes 3 faits, reformulés. **Redondance
    totale, aucune information perdue en le retirant.**
  - **06 — Football Video Analysis** : `sub` = "Detection, tracking and
    tactical analysis… CPU-only constraint" redit le `hook` = "A
    detection-and-tracking pipeline… under a CPU-only constraint" — et le
    `thesis` du même slide ("published as work in progress — not a finished
    product") redit *aussi* le `hook` ("published as work in progress"). Trois
    phrases consécutives pour deux faits (statut "en cours", contrainte CPU).
    **`sub` retiré, `thesis` conservé** (il est le premier des deux doublons
    dans l'ordre de lecture, et seul `thesis` porte l'accent visuel `pc-thesis`
    qui identifie la carte comme un "cover").
  - **01 — Football Data Pipeline** : `sub` ajoute "reproducible and
    falsifiable", un cadrage absent de la copie adjacente. **Conservé.**
  - **03 — Retirement Sustainability Model** : `sub` ajoute un fait narratif
    absent ailleurs (rapport publié le 11 juin, reconstruit "en quelques
    jours"). **Conservé.**

## Décision

Un seul chantier ce cycle, ciblé et mesuré : retrait du champ `sub` sur les
cover slides de 02 et 06 (EN+FR), qui ne satisfaisaient pas le test de rotation
A ("qu'est-ce que je peux supprimer sans perdre d'information ?"). 01 et 03
laissés intacts après la même vérification, faute de redondance réelle.
