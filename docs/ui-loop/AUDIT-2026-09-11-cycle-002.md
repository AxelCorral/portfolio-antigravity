# Audit — Cycle 002 — 2026-09-11

> Contexte : à la reprise de ce cycle, `git status` montrait 4 fichiers déjà
> modifiés et non commités (`OnePage.tsx`, `PortfolioMotion.tsx`,
> `language.tsx`, `index.css`). Diff analysé en détail : il s'agit du travail
> engagé pour traiter exactement le point de reprise noté en fin de cycle 001
> (`AnimatedLetter` reduced-motion, fenêtre de révélation du titre, footer,
> contraste du toggle de langue), mais la session précédente s'est arrêtée
> avant de commiter et de journaliser. Ce cycle a donc consisté à **vérifier,
> finir et journaliser** ce travail plutôt qu'à repartir d'un audit à blanc.

## Rotation de questions — E (Mobile-first réel)

Appliquée sur les screenshots mobile-390 générés après les corrections
(run `2026-09-11T11-34-08-514Z`) :

- **Sections du bas pensées ou juste empilées sur 390px ?** Le footer est
  désormais pensé pour mobile : bloc marque + mentions centré, liens en
  ligne (Email / GitHub / CV) avec `flex-wrap`, bouton "Back to top" en
  pilule centrée sous les liens. Ce n'est pas un simple empilement du layout
  desktop — la disposition est réellement recomposée en colonne centrée.
  Le panneau contact (3 liens) reste lisible et sans coupure.
- **Zones tactiles ≥ 44px ?** `.site-footer-links a` a `min-height: 44px`
  (vérifié dans le CSS ajouté). Les 3 cartes de lien contact
  (`.contact-panel` links) étaient déjà conformes avant ce cycle. Le bouton
  "Back to top" (`padding: 0.6rem 1.1rem` + contenu icône+texte) dépasse
  visuellement 44px de hauteur — à mesurer précisément au prochain cycle
  avec le script d'audit (pas encore instrumenté pour les hitbox du footer).
- **Scroll horizontal parasite ?** Aucun observé sur les captures pleine
  page mobile-390 (run en cours au moment de la rédaction — à confirmer avec
  `report.json`, section overflow, une fois le run terminé).
- **Constat additionnel (P1 déjà connu, non retraité ce cycle)** : les 4
  cartes "Capabilities" restent visuellement identiques sur mobile avec un
  vide non maîtrisé en bas de carte (ex. "Data Engineering", "Analytical
  Projects") avant le bouton "Learn more" — confirmé toujours présent après
  ce cycle, reste en P1 (BACKLOG.md).

## Constats visuels ciblés (avant/après)

- `section-about-analytical-profile.png` (laptop-1440, reduced-motion) :
  le paragraphe piloté par `AnimatedLetter` s'affiche désormais à opacité
  pleine et lisible d'emblée — confirmé visuellement, corrige P0-1 (audit
  cycle 001).
- `scroll-11-y4400.png` (laptop-1440, motion normale) : la carte "Analytical
  profile" entre dans le viewport avec le titre en cours de révélation
  progressive (mots du haut déjà nets, mots du bas encore en transition) —
  comportement attendu, fenêtre resserrée (marge `useInView` 240px, durée
  0.55s/délai 0.045s) pour terminer avant sortie de viewport à vitesse de
  scroll normale. Non revérifié en scroll continu rapide réel (limite de
  l'outil : captures pas-à-pas avec attente, pas un scroll fluide simulé) —
  amélioration mesurable mais pas une preuve empirique à 100 % ; à surveiller
  au cycle suivant si le problème réapparaît visuellement.
- `desktop-1920_en/scroll-21-y8400.png` et `mobile-390_en/scroll-25-*.png` :
  footer de clôture présent, identité propre (nom, mentions, liens, retour
  en haut), point final net après le panneau de contact — corrige P0-3.
- Vérification manuelle hors script (preview server dédié, port 4321) après
  resserrage de `.contact-section` (`padding-bottom` 6rem → 3.5rem) : l'écart
  mort entre le panneau contact et le footer sur mobile-390 est visiblement
  réduit sans que les deux blocs se touchent — capture temporaire supprimée
  après vérification (ne pas polluer le dossier `screenshots/`).

## Contraste — calcul de vérification (bouton de langue inactif)

`rgba(225, 224, 204, 0.58)` sur fond noir (`#000`) : luminance relative
calculée ≈ 0.2207, contraste ≈ **5.41:1** (seuil AA texte normal : 4.5:1) —
marge suffisante même si le fond réel varie légèrement (élément en
`position: fixed`, peut survoler des zones non strictement noires). À
confirmer par le scan axe-core du run en cours (`report.json`).

## État du run `scripts/ui-audit.mjs`

Run `2026-09-11T11-34-08-514Z` lancé après les 4 commits de ce cycle, sur le
code final (post-resserrage d'espacement inclus, car build relancé avant la
fin du run). Résultats détaillés (axe-core, CLS, overflow) à reporter dans
`PROGRESS.md` du cycle une fois `report.json` disponible — le run couvre les
4 viewports × 2 langues + hover + focus-visible + reduced-motion, capture
pas à pas complète (~20-25 min).
